[phases.build]
cmds = [
  "python -m nltk.downloader punkt punkt_tab averaged_perceptron_tagger averaged_perceptron_tagger_eng stopwords wordnet vader_lexicon"
]

[start]
cmd = "gunicorn --chdir src app:app --bind 0.0.0.0:$PORT"
